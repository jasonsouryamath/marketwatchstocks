const addgen = require('rrad');
var parser = require('parse-address');

const FullAddress = addgen.addresses[Math.floor(Math.random() * addgen.addresses.length)];
const obj = JSON.stringify(FullAddress);
const obj2 = JSON.parse(obj)
zipcode = obj2.postalCode;
console.log(obj2.address1);
console.log(obj2.state);
console.log(obj2.postalCode);
